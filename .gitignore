# node
node_modules/
dist/
*.tsbuildinfo

# rust
target/

# stellar cli local settings
.soroban/
.stellar/

# secrets — .env.example is the versioned template, .env.local never is
.env
.env.local
.env.*.local

# demo artefact from the README walkthrough
credential.jws

# raw output of `pnpm run acceptance:f9` — public data only, curated into evidencia/ by hand
.f9-acceptance/


# os
.DS_Store

# Vitrinee's local runtime state: order records, last receipt, mock stock
.vitrinee/
