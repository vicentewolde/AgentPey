# Evidencia — T98 (Vitrinee se fusiona en AgentPey)

Fecha: 2026-09-23 · Rama: `cc/t98-vitrinee`

Salidas crudas. Sin secretos: solo cuentas públicas, ids de contrato y hashes.

---

## 1. El punto de partida

`main` local iba dos commits adelante de `origin` (T97, mergeado la noche
anterior, con "pushear `main`" pendiente en `AGENT_LOG.md`). Se pusheó antes de
abrir la rama:

```
$ git push origin main
   8202862..66f84f8  main -> main
$ git rev-list --left-right --count HEAD...origin/main
0	0
```

## 2. Reubicar antes de fusionar (lado Vitrinee)

Rama local `agentpey-merge` en el repo Vitrinee, a partir de
`day-3-jumpseller-catalog`. Solo `git mv` y los arreglos de rutas que eso obliga.

```
$ git diff --cached --name-status -M | awk '{print substr($1,1,1)}' | sort | uniq -c
   4 A
  17 D
   3 M
 118 R
```

Los 4 `A` son archivos reescritos cuya similitud bajó de 50% (el workflow de CI,
`INSTRUCCIONES.md`, dos `vitest.config.ts`); su par está entre los `D`. Los otros
13 `D` son archivos de la raíz que AgentPey ya tiene: `package.json`,
`pnpm-lock.yaml`, `pnpm-workspace.yaml`, `tsconfig*.json`, `vitest.config.ts`,
`.gitignore`, `.editorconfig`, `.prettierrc.json`, `LICENSE` (idéntica), y el
`Cargo.toml` y `rust-toolchain.toml` del workspace de contratos (idéntico al de
AgentPey).

### Un error propio, atajado antes del commit

Al borrar el `.gitignore` de Vitrinee, un `git add -A` metió al staging 32.235
archivos ignorados, **incluido `.env.local` con secretos**. No se commiteó nada.
Se deshizo con `git reset` y se volvió a agregar con las reglas del `.gitignore`
original puestas en `.git/info/exclude`, que es solo local y nunca se commitea:

```
=== ¿secretos o basura? ===
ninguno
```

## 3. El merge

```
$ git merge --allow-unrelated-histories --no-ff vitrinee/agentpey-merge
=== conflictos ===
(ninguno)
f1b1364 merge: bring Vitrinee into AgentPey with its full history (T98)
7f08999 chore: relocate Vitrinee to its place inside AgentPey before the merge
a3e9101 docs(readme): say who each screen is for
commits de Vitrinee ahora en la historia de AgentPey: 20
```

## 4. El lockfile no movió nada de AgentPey

`pnpm install` cambió la entrada de diez paquetes de AgentPey. Revisado: solo el
sufijo de peer-dependencies de `debug`, porque `supports-color` entró al grafo
con eslint. Comprobado versión por versión, quitando los sufijos:

```
dependencias de AgentPey con VERSIÓN distinta: 0
```

`@stellar/stellar-sdk` sigue en 17.0.1 y `@x402/*` en 2.24.0 para AgentPey.
Vitrinee usa 17.1.0 y 2.26.0: conviven como paquetes separados.

## 5. Tests

### Vitrinee, desde la raíz de AgentPey

```
$ pnpm run vitrinee:check
packages/vitrinee-core test:       Tests  31 passed (31)
packages/vitrinee-adapters test:   Tests  23 passed (23)
packages/vitrinee-anchor test:     Tests  10 passed (10)
packages/vitrinee-gateway test:    Tests  27 passed (27)
apps/vitrinee-agent test:          Tests  8 passed (8)
 (scripts/vitrinee)                Tests  10 passed (10)
--- exit: 0 ---
```

109 de 109, igual que antes de la fusión. `typecheck` y `lint` limpios.

### AgentPey completo, para confirmar que no se rompió nada

```
$ pnpm typecheck   → exit 0
$ pnpm test        → exit 0
apps/agent            536 passed
apps/web              246 passed
apps/realops          159 passed
packages/partner-api  157 passed
packages/core          80 passed
packages/mandate       64 passed
apps/signaldesk        48 passed
packages/sdk           34 passed
packages/vault         32 passed
packages/cli           29 passed
packages/directory     28 passed
apps/gateway           18 passed
packages/activity      15 passed
packages/partner-sdk   14 passed
apps/status-dashboard  13 passed
packages/tenancy        9 passed
packages/webhooks       9 passed
(raíz, scripts)        64 passed
```

Las dos líneas de `stderr` que aparecen en la salida completa (`postgres-vault`
y `[webhooks] delivery … failed unexpectedly`) son de tests que simulan caídas a
propósito.

### Contratos

```
$ cd contracts/receipt-registry && cargo test
test result: ok. 11 passed; 0 failed
$ cd contracts && cargo test
test result: ok. 22 passed; 0 failed
test result: ok. 32 passed; 0 failed
```

**Observación, no causada por T98:** correr `cargo test` en `contracts/`
reescribe 30 snapshots de `policy-rail`, un valor de 32 bytes por archivo: una
llave o firma aleatoria que el test genera en cada corrida. No depende del cambio
de `contracts/Cargo.toml`. Se restauraron con `git checkout` para no commitear
ruido. Si molesta, fijar la semilla de esos tests es un arreglo aparte.

## 6. Vitrinee corre desde AgentPey, no solo compila

### El recibo de la compra real del 23 de septiembre, verificado desde acá

```
$ pnpm run vitrinee:verify
  recibo       /Users/vicentewolde/dev/AgentPay/.vitrinee/last-receipt.jws
  pedido       ord_mue4x2l7d170751c7e · 1 × Pack de stickers Cordillera · 1.0421053 USDC
  hash         7e2aa07c324d7af4476289ecac7e707cf678450663c82ea629e7542011948de6
  ✅ firma      Ed25519 de GCT7ODWJ7PF3JVWYOFJYUENQBHG6B267LIY4HWZU3BVI66W2TMURCXTW (did:stellar del merchant)
  ✅ anclaje    receipt-registry CADILO6QYG3CT2PXEWIKOYLUACPXEP4P645L5HF6WVI2K7BSVN23ZTM5 · ledger 4829257
  ✅ pago       tx de settlement confirmada en Stellar · ledger 4829256
  ✅ RECIBO VÁLIDO
```

Esto ejercita las tres rutas reubicadas: `.env.vitrinee.local`,
`deployments/vitrinee-testnet.json` y `.vitrinee/`.

### El gateway, contra la tienda Jumpseller real

```
$ ADAPTER=jumpseller PORT=4031 pnpm run vitrinee
health:    200
dashboard: 200
payTo   : GC5ZY7UJ7CKD7O7YURRSDIDVYEETYP2JXPKUL5E6GIWHUPAH5DCIVCII
did     : did:stellar:testnet:GCT7ODWJ7PF3JVWYOFJYUENQBHG6B267LIY4HWZU3BVI66W2TMURCXTW
ids     : ['37282902', '37282997', '37282998'] ...
"adapter":"jumpseller"
```

Las cuentas del merchant son las de Vitrinee: leyó `.env.vitrinee.local`, no el
`.env.local` de AgentPey. Los ids numéricos son los de Jumpseller, no los del mock.

## 7. Secretos

`.env.vitrinee.local` se copió desde el repo viejo con modo `600` y queda
ignorado por `.env.*.local`, que AgentPey ya tenía:

```
$ git check-ignore -v .env.vitrinee.local .vitrinee/orders.json
.gitignore:16:.env.*.local	.env.vitrinee.local
.gitignore:29:.vitrinee/	.vitrinee/orders.json
```
