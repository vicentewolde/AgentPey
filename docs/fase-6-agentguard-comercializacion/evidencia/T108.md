# Evidencia · T108 · Firma y recibo claros

> Rama `cc/t108-firma-recibo`, encima de `cc/t107-realops-legible`. Ticket
> Exponential `cmughle0e000pl6042nj1ies5`.

## Tests

```
$ pnpm typecheck          -> sin errores
$ pnpm test               -> exit 0
  packages/vitrinee-gateway  112 passed (eran 111)
  apps/web                   255 passed (eran 254)
  apps/realops               201 passed
```

- `checkout.test.ts` · "renders the same verification as a page a person can
  read": `text/html`, "Valid receipt: all three checks pass", tres
  `class="check ok"`, total, enlace al JSON y a Stellar Expert, sin "—"; `?lang=es`
  y `Accept-Language: es-CL` en español; hash desconocido 404.
- `portal.test.ts`: el enlace "Recibo" del panel apunta a `/receipts/<hash>`.
- `public-pages.test.ts` · "consent.html puts Connect wallet in the top bar":
  el botón está dentro de la barra, antes del encabezado; no queda
  `wallet-panel`; el botón de vuelta es `btn big`.
- `signing.test.ts`: volver con la firma completa redirige a
  `/catalogo?agente=<id>`; con la firma pendiente, a `/agentes/<id>`.

## Vista del recibo

`receiptPage` con el JSON real del recibo de T104
(`bazar-cordillera.vitrinee.agentpey.com/receipts/4a7a9771…9c9f2d/verify`, leído en
vivo el 2026-09-25), capturado a 900 px: "Recibo válido: pasan las tres
comprobaciones", tres comprobaciones en verde, "Pack de stickers Cordillera ·
1 · 1.0421053", pedido `ord_mufn0etw3d8329de61`, enlaces al pago y al JSON.

La página de firma no se probó en un navegador con Freighter: su cambio es de
maqueta y el flujo de firma (misma función `connectWallet`) no cambió.
