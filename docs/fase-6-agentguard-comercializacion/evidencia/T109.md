# Evidencia · T109 · Contratar en menos pasos

> Rama `cc/t109-menos-pasos`, encima de `cc/t108-firma-recibo`. Ticket
> Exponential `cmughv6vh0011l604343dn9zh`.

## Tests

```
$ pnpm typecheck        -> sin errores
$ pnpm test             -> exit 0
  apps/realops          213 passed (eran 201)
```

Nuevos, en `catalog-http.test.ts` ("T109: hire and sign in one step, one agent's
catalogue, search"): contratar desde la pregunta única con `store:<slug>` y
`firmar=1` redirige a `agentpey.example/consent/cns_…` con el agente atado a la
tienda; nombre vacío toma el del tipo; recontratar una tienda ya firmada lleva a
`/catalogo?agente=<id>` sin crear otro; "Contratar y firmar" desde el catálogo
también firma; `/catalogo?agente=` muestra solo sus productos y dice si puede
comprar; el id de otra cuenta muestra todo; la ficha firmada enlaza a su
catálogo; una frase sin producto conocido busca en todas las tiendas y no compra;
sin agentes, "stickers" abre su tarjeta y "un tazón" busca; la búsqueda se escapa.
En `catalog.test.ts`: `searchCards` ignora acentos, mayúsculas y plural, y no
encuentra nada en "hola que tal". Reemplazados: el test de los dos formularios
(ahora uno solo) y el que exigía esconder el cuadro de frases sin agentes.

## En el navegador (servidor local, directorio real de Vitrinee, sin firmar)

```
Mis agentes · ¿Qué quieres que haga?
  store:bazar-cordillera 25.00/25.00 | store:mycokit 25.00/25.00 |
  bazaar_shopper 0.30/0.60 | market_brief 0.30/0.60 | ai_credits 0.30/0.60
  (al elegir bazaar_shopper, los campos pasan a 0.30/0.60)
/catalogo?q=quiero un kit de hongos -> Kit de cultivo Melena de León, Reishi,
  Ostra, Cola de Pavo; cada uno con /catalogo/permiso?producto=…
/catalogo?q=stickers -> Pack de stickers Cordillera, "fuera del permiso"
Consola: sin errores.
```
