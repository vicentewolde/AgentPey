# Evidencia · T91 · `payTo` obligatorio en `POST /v1/consent_sessions`

Rama `cc/t91-require-payto`, desde `main` en `882c62d`. Sin mergear. Decisión: `C-123`, opción 2.

## 1. Diagnóstico previo

```
apps/realops/src/permissions.ts:47  payTo: z.array(z.string()).min(1),
scripts/f9-acceptance.ts:578  payTo: [PAY_TO],
apps/agent/src/policy/terms.ts:144  if (allowedPayTo !== undefined && terms.payTo !== undefined) {
apps/web/src/server.ts:704  const grant = mandateGrantSchema.parse(session.grant);
```

## 2. Contrato publicado (`git diff docs/api/openapi.yaml`)

```diff
diff --git a/docs/api/openapi.yaml b/docs/api/openapi.yaml
index e0629c3..c17fb6d 100644
--- a/docs/api/openapi.yaml
+++ b/docs/api/openapi.yaml
@@ -423,6 +423,7 @@ components:
                 - currency
               additionalProperties: false
             payTo:
+              minItems: 1
               type: array
               items:
                 type: string
@@ -436,6 +437,7 @@ components:
             - venues
             - assets
             - limits
+            - payTo
           additionalProperties: false
         valid_from:
           type: string
```

## 3. Tests

```
apps/gateway test:       Tests  18 passed (18)
packages/core test:       Tests  80 passed (80)
packages/vault test:       Tests  24 passed (24)
packages/sdk test:       Tests  34 passed (34)
packages/tenancy test:       Tests  9 passed (9)
apps/signaldesk test:       Tests  48 passed (48)
packages/directory test:       Tests  28 passed (28)
packages/mandate test:       Tests  64 passed (64)
packages/cli test:       Tests  29 passed (29)
packages/activity test:       Tests  15 passed (15)
apps/realops test:       Tests  108 passed (108)
packages/partner-api test:       Tests  103 passed (103)
apps/agent test:       Tests  503 passed (503)
packages/partner-sdk test:       Tests  6 passed (6)
apps/status-dashboard test:       Tests  13 passed (13)
apps/web test:       Tests  208 passed (208)
packages/webhooks test:       Tests  5 passed (5)
      Tests  54 passed (54)
```

Total: 1349 (eran 1344). `pnpm build` y `pnpm typecheck`: exit 0.

Tests nuevos:

```
+  it("400s a grant that names no payee, without creating anything", async () => {
+  it("rejects a grant without payTo", () => {
+  it("rejects an empty payTo", () => {
+  it("still rejects a payee that is not a Stellar account or contract", () => {
+  it("refuses a consent session whose grant names no payee, without calling the API (C-123)", async () => {
```

## 4. Archivos

```
 apps/web/src/partner-routes.test.ts                | 41 +++++++++++++++++-
 docs/api/openapi.yaml                              |  2 +
 .../fase-6-agentguard-comercializacion/BITACORA.md | 50 ++++++++++++++++++++--
 .../DECISIONES.md                                  |  5 ++-
 examples/cloudops-partner-integration.md           |  8 +++-
 .../src/resources/consent-sessions.test.ts         | 31 ++++++++++++++
 .../partner-api/src/resources/consent-sessions.ts  | 20 ++++++---
 packages/partner-sdk/src/index.test.ts             | 20 +++++++++
 8 files changed, 163 insertions(+), 14 deletions(-)
```
