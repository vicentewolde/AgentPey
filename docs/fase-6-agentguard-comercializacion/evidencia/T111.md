# Evidencia · T111 · pnpm run rail:topup

> Rama `cc/rail-topup`. Ticket Exponential `cmuh17lc20001jm04rxvki5s9`.

```
$ pnpm typecheck                 -> sin errores
$ pnpm test                      -> exit 0 (scripts: 74 -> 80 tests)
scripts/lib/topup-args.test.ts   6 passed
```

Rechazos comprobados corriendo el comando real, sin ninguna llave:

```
rail:topup -- GAK6E5E7…GFP2K 5   -> InvalidArguments: the first argument must be a contract address (C...)
rail:topup -- CCNA6ACL…AKC45L 500 -> InvalidArguments: the amount must be above 0 and at most 20 USDC per command
rail:topup -- CCNA6ACL…AKC45L diez -> InvalidArguments: the amount must be a plain decimal
rail:topup                        -> InvalidArguments: usage: …
```

**Lo que Claude Code no pudo probar:** el envío. Necesita `AGENT_SECRET_KEY`, que
Claude Code no lee. El camino de envío repite el `transfer` que ya usa
`apps/web/src/tenant-rail.ts` para fondear un rail nuevo (mismo cliente y misma
firma), pero esta herramienta no se ha ejecutado contra la red. Sin `--yes` solo
lee saldos, y es lo primero que debe correr el usuario.

Contexto del saldo (datos públicos, 2026-09-25 13:50 UTC): rail
`CCNA6ACL4L76EDJZXEFAFDJQPWW3CJXZUQFS6IL574E5Z6AOLXAKC45L`, creado 12:35:12Z,
fondeado por la reserva con 3 USDC a las 12:35:17Z, saldo 0.9052632 USDC tras pagar
el Kit Cola de Pavo (2.0947368).
