# Evidencia · T110 · Vitrinee no publica productos con SKU repetido

> Rama `cc/t110-sku-repetido`. Ticket Exponential `cmugzc1cj000njp042cuod04w`.

```
$ pnpm --filter @vitrinee/adapters exec vitest run
      Tests  24 passed (24)
$ pnpm run vitrinee:check   -> typecheck, lint y tests en verde
$ pnpm test                 -> exit 0
```

Test nuevo en `packages/vitrinee-adapters/src/jumpseller/jumpseller.test.ts`:
"leaves out every product whose SKU another product shares, and says so": tres
productos con `demo-product` (uno con espacios) quedan fuera, el hoodie se publica,
un duplicado deshabilitado del hoodie no lo saca, y `onWarning` recibe
`{ skus: ["demo-product"] }`.

Las dos tiendas vivas no cambian: Bazar Cordillera (6) y MycoKit (4) tienen SKU
distintos (`evidencia/T105.md` § 6.3).

La primera corrida de `pnpm test` en esta rama falló en
`scripts/lib/refusal-codes.test.ts`: el texto de `RailInsufficientFunds` cambió
en `cc/t107-saldo` sin regenerar `CODIGOS-DE-RECHAZO.md`. Regenerado con
`pnpm run docs:refusal-codes`; después, `pnpm test` en verde.

## En vivo (2026-09-25)

Mergeado a `main` (`86d6604`, push 13:17 UTC). Revisado a las 13:24 UTC:

```
bazar-cordillera 6 productos · mycokit 4 productos (sin cambios: sus SKU son únicos)
agentpey.com, realops.agentpey.com, vitrinee.agentpey.com: 200
recibo de T104 como página: 3 comprobaciones "check ok"
```

Limitación: no hay endpoint de versión, así que "desplegado" se infiere del tiempo
transcurrido (siete minutos; el deploy anterior tardó unos cinco). El filtro no
cambia nada visible en las tiendas actuales: se notará con la tienda nueva del
video.
