# Evidencia · T104 · AgentPey y RealOps leen el directorio de comercios de Vitrinee

> Rama `cc/t104-directorio-comercios`. Salidas crudas, sin secretos.

## 1. Después del deploy de cierre de T103 (pendiente de T103, anotado aquí)

Siete minutos después del merge de `feca377`, sin tocar nada:

```
health: {"status":"ok","mode":"platform","platformHost":"vitrinee.agentpey.com","rootComercio":"bazar-cordillera"}
comercio: Bazar Cordillera 6 products
order: paid_unfulfilled 9.4631579 b961157ce416
```

## 2. El hallazgo que habría roto la compra

`toPaymentTerms` (`apps/agent/src/payment/x402.ts`) resolvía el activo del 402
con `mapAssetContract`, que lee `venues.json` fijo. Un comercio de plataforma
solo existe en el registro expandido, así que la compra habría fallado con
`InvalidProduct` justo al pagar. Ahora recibe el registro expandido y, además,
fija el `payTo` de un comercio de plataforma a la cuenta de su id (`C-145`).

## 3. Contrato real entre los dos lados, sin red

`scripts/vitrinee/agentpey-contract.test.ts`, test nuevo: la app de plataforma
real de Vitrinee (un comercio `bazar`), el código real de AgentPey leyendo su
directorio con `expandPlatformVenues`, catalogando la tienda con
`createX402Catalog`, pidiendo el 402 con `requestPaymentChallenge` y
reconciliando con `toPaymentTerms`:

```
✓ scripts/vitrinee/agentpey-contract.test.ts (6 tests)
```

## 4. Pruebas

```
apps/agent          Tests 550 passed   -- eran 540: expansión del directorio y registro con plataformas (8), payee fijado (2)
apps/web            Tests 254 passed   -- eran 250: compra a comercio de plataforma (4)
apps/realops        Tests 187 passed   -- eran 175: catálogo por tienda (5), HTTP (5), lector del directorio (4)
packages/vitrinee-gateway Tests 69 passed -- eran 68: el directorio público
apps/gateway        Tests 32 passed
scripts (raíz)      Tests 74 passed
pnpm typecheck      ok
pnpm run vitrinee:lint limpio
```

## 5. Pendiente de verificar en vivo

- `https://vitrinee.agentpey.com/api/comercios` con Bazar Cordillera.
- RealOps: la sección "Tiendas en Vitrinee" con Bazar Cordillera; contratar un
  comprador desde una tarjeta y firmar su permiso (el usuario, con su wallet).
- Una compra en testnet resuelta por el directorio, con los tres checks del
  recibo en verde. El pedido en Jumpseller sigue dependiendo de su `404`
  (T101).
