# Evidencia · T107 · RealOps se entiende

> Rama `cc/t107-realops-legible`, desde `origin/main` (`c343b62`). Ticket
> Exponential `cmugh4ji00005l6049a7lvjl9`.

## Tests

```
$ pnpm --filter @agentpey/realops exec vitest run
      Tests  201 passed (201)
$ pnpm typecheck   -> sin errores
```

Ocho tests nuevos en `apps/realops/src/purchasing.test.ts` ("T107: what was
bought, and what just happened"): nombre del producto en entrega y rechazo con el
id debajo; id cuando el catálogo ya no lo lista; tras un rechazo, la redirección
a `/servicios?compra=<id>` y el aviso `role="alert"` con el motivo y "No se pagó
nada"; tras una compra, "Comprado: …" sin alerta; "Tu pedido se envió" si AgentPey
aún no la lista; un parámetro que no es id se ignora; las más nuevas primero y
"Ver 2 más" pasada la sexta; la cuenta por su email y el código plegado.

## Vista

La página de servicios, armada con `servicesPage` y datos de ejemplo (un rechazo
recién pedido y ocho entregas), capturada con Chrome sin interfaz a 1400 px: aviso
rojo arriba con "Refused: Kit de cultivo Cola de Pavo", entregas con nombre en
tres columnas, "See 2 more", y "Signed in as vicente@ejemplo.cl (Vicente)".

## En vivo (2026-09-25)

Mergeado a `main` en fast-forward con T107, T108 y T109 juntos (`d496ba9`, push
05:03 UTC). Render terminó cuando `/receipts/<hash>` pasó de 404 JSON a 200 HTML.

Las pantallas de RealOps piden sesión por email: Claude Code no puede abrirlas en
producción. Probadas en tests y en el servidor local contra el directorio real;
falta que el usuario las vea en vivo para pasar a `DONE`.

## Rechazo por saldo (2026-09-25)

El usuario contrató un comprador de MycoKit y pidió la Melena de León: rechazado.
Diagnóstico con datos públicos, sin claves:

```
MycoKit /checkout/37309503 -> 402, 2.6210526 USDC, payTo GD2M…K5GN (la tienda responde bien)
Reserva GAK6E5E7…GFP2K: 141.484 USDC (no es la reserva)
04:18:42Z reserva -> 3 USDC a un rail nuevo CARSN6GT2AJFWEBKCCEVPLVO2AWR73ZRTUSJMQQR3NR46ZA6EGU6MZEV
04:18:52Z ese rail -> 1.0421053 USDC a Bazar Cordillera (stickers)
saldo del rail: 19578947 = 1.9578947 USDC < 2.6210526
rail de la cuenta anterior CDELNDQ…ASDI: 12.4947368 USDC, sin cambios
```

Arreglo: el texto de `RailInsufficientFunds` ya no promete una recarga, y el
aviso muestra el saldo. Test nuevo en `purchasing.test.ts`; RealOps 214.
