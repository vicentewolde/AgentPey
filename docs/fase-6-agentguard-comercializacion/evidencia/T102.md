# T102 · Evidencia cruda

Rama `cc/t102-vitrinee-deploy`, desde `main` en `d9ed9a5`. Fecha 2026-09-23.
Ningún secreto aparece abajo.

## 1. Antes: el deploy viejo no sirve al comprador de AgentPey

Ver `evidencia/T100.md` § 1: `https://vitrinee-gateway.onrender.com/api/discovery/search`
responde `404`.

## 2. Tests

```
$ pnpm --filter @agentpey/gateway test
 ✓ src/env-filter.test.ts (7 tests)
 ✓ src/hosts.test.ts (15 tests)
 ✓ src/proxy.test.ts (3 tests)
 ✓ src/supervisor.test.ts (2 tests)
      Tests  27 passed (27)          (eran 18)
$ pnpm typecheck                     (sin errores)
$ pnpm test                          (exit 0)
```

Los de aislamiento (`hosts.test.ts`, "which secrets each child actually
receives") arman el entorno de cada hijo con la tabla real `APP_TARGETS` y un
contenedor con todos los secretos: Vitrinee recibe sus valores con el nombre sin
prefijo y ninguno de AgentPey; ninguna otra app recibe la llave de firma de
recibos, la clave del facilitator ni las credenciales de Jumpseller; ningún
`envKeys` de otra app nombra un `VITRINEE_` secreto.

## 3. En local, con las piezas reales del gateway

Arnés en el scratchpad (no versionado): importa `hosts.ts`, `supervisor.ts` y
`proxy.ts` del repo, arma el contenedor como lo tendrá Render (cada línea de
`.env.vitrinee.local` con prefijo `VITRINEE_`, más
`VITRINEE_PUBLIC_BASE_URL=https://vitrinee.agentpey.com` y un
`AGENT_SECRET_KEY` de mentira que no debe llegar), arranca Vitrinee con
`spawnApp` y enruta por `Host` con `resolveTarget` y `proxyRequest`. No se
levantaron web, RealOps ni SignalDesk: el `DATABASE_URL` local apunta a una
base remota.

Con los secretos:

```
missing: []
[vitrinee] {"message":"vitrinee gateway listening","port":4104,"adapter":"jumpseller","merchant":"GC5ZY7UJ…DCIVCII",…}
== manifest (Host: vitrinee.agentpey.com)
https://vitrinee.agentpey.com/checkout/{productId} | https://vitrinee.agentpey.com/discovery/resources | GC5ZY7UJ7CKD7O7YURRSDIDVYEETYP2JXPKUL5E6GIWHUPAH5DCIVCII
== discovery
6 cards
37283001 {'asset': 'USDC', 'amount': '1.0421053', 'destination': 'GC5ZY7UJ7CKD7O7YURRSDIDVYEETYP2JXPKUL5E6GIWHUPAH5DCIVCII'}
== checkout GET
402
== child env: AGENT_SECRET_KEY value present? 0; VITRINEE_* names? 0; PUBLIC_BASE_URL=https://vitrinee.agentpey.com
```

Sin `VITRINEE_MERCHANT_SIGNING_SECRET`:

```
missing: [VITRINEE_MERCHANT_SIGNING_SECRET]
vitrinee is not running on this deployment
HTTP 503
vitrinee listening? 0
```

## 4. En vivo

Dominio `vitrinee.agentpey.com` agregado en Render (Settings → Custom Domains,
0,25 USD/mes, con el sí del usuario) y verificado contra el CNAME
`vitrinee` → `agentpey.onrender.com` que el usuario creó en Vercel.

Primer deploy de `main` en `c2eaae4`, ~110 s después del push:

```
agentpey.com: 200
realops.agentpey.com: 200
signaldesk.agentpey.com: 200
vitrinee.agentpey.com: 503
vitrinee is not running on this deployment
```

Log del servicio en Render (primer deploy):

```
gateway: not starting vitrinee, missing VITRINEE_MERCHANT_STELLAR_ACCOUNT
```

El servicio no sincroniza `render.yaml`: solo llegaron los cuatro secretos que
cargó el usuario. Faltan en el panel las variables públicas. Arreglo en
`cc/t102-cierre`: `VITRINEE_ADAPTER` obligatoria (tests del gateway 28).

## 5. Segundo intento: sin memoria, y el plan nuevo

Con las variables cargadas, dos deploys fallaron:

```
gateway: waiting for web:4101, realops:4102, signaldesk:4103, vitrinee:4104...
==> Out of memory (used over 512Mi)
```

Medición local de Vitrinee sola: `tsx src/main.ts` 97 MB (3 procesos),
`node dist/main.js` 138 MB. Sin mejora posible por ahí. Metrics del servicio
antes de Vitrinee: 88 % a 93 % de 512 MB.

Acciones en Render (con Claude en Chrome): `VITRINEE_ADAPTER` borrada y
deploy de `8c3d5e3` entra con Vitrinee sin arrancar (503, resto en 200);
plan `0.5c-512mb` → `1c-2g` (25 USD al mes, `C-138`); `VITRINEE_ADAPTER=jumpseller`
vuelta a cargar.

## 6. En vivo, `main` en `8c3d5e3`

```
agentpey.com: 200 · realops.agentpey.com: 200 · signaldesk.agentpey.com: 200
vitrinee.agentpey.com/.well-known/agent-storefront.json → 200 (~120 s tras el deploy)
  checkout https://vitrinee.agentpey.com/checkout/{productId}
  discovery https://vitrinee.agentpey.com/discovery/resources
  merchant GC5ZY7UJ7CKD7O7YURRSDIDVYEETYP2JXPKUL5E6GIWHUPAH5DCIVCII
GET /api/discovery/search?query=*  → 6 cards: 37282902 36.8315789 · 37282997 15.7789474 ·
  37282998 13.6736842 · 37282999 9.4631579 · 37283000 21.0421053 · 37283001 1.0421053
GET /checkout/37283001?quantity=1&name=…  → HTTP/2 402 con payment-required
```

Memoria del servicio con los cuatro procesos: ~22 % de 2 GB (Metrics de Render).
